# C4 Model - tree-sitter-objectscript

> Update note (2026): `objectscript_routine` is now a first-class grammar path (`expr -> core -> objectscript_routine`) alongside the `expr -> core -> objectscript_udl -> objectscript` chain. See `README.md` and `CONTRIBUTING.md` for current workflows and commands.

## Overview

This document describes the architecture of tree-sitter-objectscript using the C4 model approach, covering system context, container, and component levels.

---

## Level 1: System Context

The tree-sitter-objectscript project provides parsing infrastructure for InterSystems ObjectScript, enabling syntax highlighting, code analysis, and structural editing in code editors.

```mermaid
C4Context
    title System Context - tree-sitter-objectscript

    Person(developer, "ObjectScript Developer", "Writes ObjectScript code in editors")
    
    System(tsObjectScript, "tree-sitter-objectscript", "Parser generator for ObjectScript syntax")
    
    System_Ext(zed, "Zed Editor", "Modern code editor")
    System_Ext(neovim, "Neovim", "Terminal-based editor with nvim-treesitter")
    System_Ext(emacs, "Emacs", "Extensible editor with tree-sitter support")
    System_Ext(tsCli, "tree-sitter CLI", "Parser generator and build tooling")
    System_Ext(irisDb, "InterSystems IRIS", "Data platform using ObjectScript")
    
    Rel(developer, tsObjectScript, "Uses parsers for")
    Rel(tsObjectScript, zed, "Provides ObjectScript extension to")
    Rel(tsObjectScript, neovim, "Provides parser to nvim-treesitter in")
    Rel(tsObjectScript, emacs, "Provides major mode to")
    Rel(tsCli, tsObjectScript, "Generates parsers from grammar")
    Rel(developer, irisDb, "Develops applications for")
```

### Narrative

Developers working with InterSystems IRIS use ObjectScript as their primary language. The tree-sitter-objectscript project generates parsers from grammar definitions that integrate with modern editors (Zed, Neovim, Emacs) to provide real-time syntax highlighting, structural editing, and code analysis. The tree-sitter CLI is used to generate and test parsers from the grammar specifications.

---

## Level 2: Container Diagram

The project contains four layered grammars and language bindings for multiple platforms.

```mermaid
    C4Container
    title Container Diagram - tree-sitter-objectscript

    Container_Boundary(grammars, "Grammar Layer") {
        Container(expr, "expr Grammar", "JavaScript/tree-sitter", "Handles ObjectScript expressions: literals, operators, variables, function calls")
        Container(core, "core Grammar", "JavaScript/tree-sitter", "Core ObjectScript statements and commands; extends expr")
        Container(udl, "objectscript_udl Grammar", "JavaScript/tree-sitter", "UDL class definition syntax for .cls files; extends core")
        Container(playground, "objectscript Grammar", "JavaScript/tree-sitter", "Playground grammar; extends objectscript_udl and allows top-level mixed snippets")
    }
    
    Container_Boundary(shared, "Shared Components") {
        Container(common, "common", "JavaScript/C", "Shared identifiers, scanner, grammar utilities")
        Container(queries, "Query Files", "Scheme", "Highlights, injections, indents queries")
    }
    
    Container_Boundary(bindings, "Language Bindings") {
        Container(rust, "Rust Binding", "Rust/Cargo", "Native Rust crate")
        Container(python, "Python Binding", "Python/pip", "Python wheel package")
        Container(node, "Node Binding", "JavaScript/npm", "Node.js package")
        Container(go, "Go Binding", "Go module", "Go module")
        Container(swift, "Swift Binding", "Swift Package", "Swift package")
        Container(c, "C Binding", "C headers", "Native C library")
    }

    Rel(core, expr, "Extends")
    Rel(udl, core, "Extends")
    Rel(playground, udl, "Extends")
    Rel(expr, common, "Uses identifiers from")
    Rel(core, common, "Uses scanner from")
    Rel(udl, common, "Uses grammar helpers from")
    Rel(playground, common, "Uses scanner/helpers from")
    
    Rel(rust, playground, "Wraps")
    Rel(rust, udl, "Also wraps")
    Rel(python, playground, "Wraps")
    Rel(python, udl, "Also wraps")
    Rel(node, playground, "Wraps")
    Rel(node, udl, "Also wraps")
    Rel(go, playground, "Wraps")
    Rel(go, udl, "Also wraps")
    Rel(swift, playground, "Wraps")
    Rel(swift, udl, "Also wraps")
    Rel(c, playground, "Wraps")
    Rel(c, udl, "Also wraps")
```

### Narrative

The architecture uses a **layered grammar design**:

1. **expr** (base): Handles pure ObjectScript expressions—literals, operators, variable references (`lvn`, `gvn`, `ssvn`), system functions (`$PIECE`, `$LIST`, etc.), macros (`$$$`), and JSON literals.

2. **core** (extends expr): Adds statement-level constructs—commands (`SET`, `DO`, `FOR`, `IF`, etc.), control flow, embedded SQL/HTML/XML/JavaScript, preprocessor directives (`#define`, `#if`), and routine/tag definitions.

3. **objectscript_udl** (extends core): Adds class definition syntax for `.cls` files—class declarations, methods, properties, parameters, queries, triggers, XDATA blocks, indices, and storage definitions.

4. **objectscript** (extends objectscript_udl): Adds playground/sandbox top-level parsing so routine statements and class-member constructs can be parsed in mixed or partial snippets.

This layered approach enables reuse: the `expr` grammar can be injected into SQL dialects or CSP templates independently, `core` handles routine files, `objectscript_udl` handles class files, and `objectscript` handles playground/snippet input.

---

## Level 3: Component Diagram

### Grammar Components

```mermaid
C4Component
    title Component Diagram - expr Grammar

    Container_Boundary(expr_grammar, "expr Grammar") {
        Component(literals, "Literals", "Rules", "string_literal, numeric_literal, json_*_literal")
        Component(operators, "Operators", "Rules", "binary_operator, unary_operator, pattern_operator")
        Component(variables, "Variables", "Rules", "lvn, gvn, ssvn, instance_variable, sql_field_reference")
        Component(functions, "System Functions", "Rules", "system_defined_function, system_defined_variable, dollar_*")
        Component(classes, "Class Refs", "Rules", "class_method_call, class_parameter_ref, class_ref")
        Component(oref, "Object References", "Rules", "oref_chain_expr, oref_method, oref_property")
        Component(macros, "Macros", "Rules", "macro, macro_constant, macro_function")
        Component(extrinsic, "Extrinsic Functions", "Rules", "extrinsic_function, line_ref, routine_ref")
    }

    Rel(operators, literals, "Operates on")
    Rel(operators, variables, "Operates on")
    Rel(functions, literals, "Takes as arguments")
    Rel(oref, variables, "Chains from")
    Rel(classes, functions, "Uses similar patterns as")
```

### Narrative

The **expr grammar** decomposes ObjectScript expressions into:

- **Literals**: String (`"..."` with `""` escapes), numeric (integers, decimals, scientific), JSON (objects, arrays, booleans, null)
- **Operators**: Binary (`+`, `-`, `*`, `/`, `_`, `=`, `<`, `>`, `&`, `!`, `]`, `[`, etc.), unary (`+`, `-`, `'`), pattern (`?`, `'?`)
- **Variables**: Local (`lvn`), global (`^gvn`), structured system (`^$ssvn`), instance (`i%`, `r%`, `m%`)
- **System Functions**: Over 100 built-in functions (`$PIECE`, `$LIST`, `$GET`, `$ORDER`, `$ZDATE`, etc.) and special variables (`$HOROLOG`, `$JOB`, `$NAMESPACE`, etc.)
- **Class References**: `##class(pkg.Class).Method()` calls and parameter references
- **Object Chains**: `oref.prop.method().#Param` chains with method/property/parameter access
- **Macros**: `$$$MacroName` and `$$$MacroFunc(args)`
- **Extrinsic Functions**: `$$tag^routine(args)` and `$$@indirection`

---

### Core Grammar Components

```mermaid
C4Component
    title Component Diagram - core Grammar

    Container_Boundary(core_grammar, "core Grammar") {
        Component(commands, "Commands", "Rules", "command_set, command_do, command_for, command_if, etc.")
        Component(control, "Control Flow", "Rules", "command_while, command_trycatch, elseif_block, else_block")
        Component(io, "I/O Commands", "Rules", "command_read, command_write, command_open, command_close, command_use")
        Component(embedded, "Embedded Languages", "Rules", "embedded_sql, embedded_html, embedded_xml, embedded_js")
        Component(preproc, "Preprocessor", "Rules", "pound_define, pound_if, pound_ifdef, pound_include")
        Component(tags, "Tags/Procedures", "Rules", "tag, tag_with_params, procedure, parameter_list")
        Component(externals, "External Scanner", "C code", "Handles whitespace-sensitive parsing, SQL markers, comments")
    }

    Rel(commands, io, "Includes")
    Rel(control, commands, "Controls execution of")
    Rel(preproc, commands, "Conditionally includes")
    Rel(externals, commands, "Tokenizes for")
```

### Narrative

The **core grammar** adds:

- **Commands**: 30+ ObjectScript commands including data manipulation (`SET`, `KILL`, `MERGE`), control flow (`IF`, `FOR`, `WHILE`, `DO`), I/O (`READ`, `WRITE`, `OPEN`, `CLOSE`, `USE`), transactions (`TSTART`, `TCOMMIT`, `TROLLBACK`), and debugging (`ZBREAK`, `ZWRITE`)
- **Control Flow**: Block-style (`IF {} ELSE {}`) and old-style (`IF condition commands...`) syntax
- **Embedded Languages**: `&sql()`, `&html<>`, `&xml<>`, `&js<>` blocks with language injection support
- **Preprocessor**: `#define`, `#if`/`#ifdef`/`#ifndef`/`#else`/`#endif`, `#include`, `#import`
- **Tags/Procedures**: Routine labels with optional parameters and procedure definitions with access modifiers
- **External Scanner**: C-based scanner (`common/scanner.h`) for whitespace-sensitive disambiguation, embedded language markers, and comment handling

---

### objectscript_udl Grammar Components

```mermaid
C4Component
    title Component Diagram - objectscript_udl Grammar

    Container_Boundary(udl_grammar, "objectscript_udl Grammar") {
        Component(class_def, "Class Definition", "Rules", "class_definition, class_extends, class_body, class_keywords")
        Component(methods, "Methods", "Rules", "method, classmethod, method_definition, arguments, return_type")
        Component(properties, "Properties", "Rules", "property, relationship, foreignkey, property_type, property_keywords")
        Component(parameters, "Parameters", "Rules", "parameter, parameter_type, parameter_keywords")
        Component(queries, "Queries", "Rules", "query, query_body, query_keywords")
        Component(triggers, "Triggers", "Rules", "trigger, core_trigger, external_trigger, trigger_keywords")
        Component(xdata, "XDATA Blocks", "Rules", "xdata, xdata_xml, xdata_any, xdata_keywords")
        Component(storage, "Storage", "Rules", "storage, storage_body, storage_keywords")
        Component(indices, "Indices", "Rules", "index, index_properties, index_keywords")
        Component(keywords, "UDL Keywords", "common/keywords.js", "Method/property/query/trigger keyword definitions")
    }

    Rel(methods, class_def, "Contained in")
    Rel(properties, class_def, "Contained in")
    Rel(parameters, class_def, "Contained in")
    Rel(triggers, class_def, "Contained in")
    Rel(xdata, class_def, "Contained in")
    Rel(methods, keywords, "Uses")
    Rel(properties, keywords, "Uses")
```

### Narrative

The **objectscript_udl grammar** models ObjectScript class definitions (`.cls` files):

- **Class Definition**: `Class pkg.ClassName Extends SuperClass [ Keywords ] { ... }`
- **Methods**: Instance and class methods with arguments, return types, and keyword modifiers (`[Language=python]`, `[CodeMode=expression]`, etc.)
- **Properties**: Instance properties with types, relationships, foreign keys
- **Parameters**: Class parameters with types and default values
- **Queries**: `%SQLQuery` definitions with SQL body injection
- **Triggers**: Database triggers with ObjectScript or external language bodies
- **XDATA**: Named XML/JSON/YAML/Markdown data blocks with MIME-type-based injection
- **Storage**: XML-based storage definitions
- **Indices**: Database indices on properties

---

### Playground Grammar Components

```mermaid
C4Component
    title Component Diagram - objectscript Grammar

    Container_Boundary(playground_grammar, "objectscript Grammar") {
        Component(source, "Top-level Source", "Rules", "source_file allows class_definition, class_statement, and statement at top level")
        Component(scanner_mode, "Scanner Mode", "C scanner flag", "column1_statement_mode=true for tag/statement disambiguation in snippets")
    }

    Rel(source, scanner_mode, "Relies on")
```

### Narrative

The **objectscript grammar** is the playground profile:

- Accepts top-level routine statements and class-member constructs
- Reuses UDL/core/expr rules via inheritance
- Uses scanner mode to disambiguate tags vs statements in column 1

---

## Dynamic Flow: Parsing a .cls File

```mermaid
sequenceDiagram
    participant Editor as Editor (Zed/Neovim)
    participant TS as tree-sitter Runtime
    participant UDL as objectscript_udl Parser
    participant Core as core Grammar Rules
    participant Expr as expr Grammar Rules
    participant Scanner as External Scanner
    participant Inject as Injection Queries

    Editor->>TS: Open Sample.cls file
    TS->>UDL: Parse source text
    UDL->>Scanner: Tokenize (handle whitespace, comments, markers)
    Scanner-->>UDL: Token stream
    
    UDL->>UDL: Match class_definition
    UDL->>UDL: Match method_definition
    UDL->>Core: Parse method body (statements)
    Core->>Expr: Parse expressions in statements
    Expr-->>Core: Expression AST nodes
    Core-->>UDL: Statement AST nodes
    
    UDL->>UDL: Match xdata block
    UDL-->>TS: Complete AST
    
    TS->>Inject: Apply injection queries
    Inject->>Inject: Detect [Language=python] → inject python
    Inject->>Inject: Detect &sql() → inject sql
    Inject->>Inject: Detect xdata MimeType → inject xml/json/yaml
    Inject-->>TS: Injection ranges
    
    TS-->>Editor: AST + injections for highlighting
```

### Narrative

1. Editor opens a `.cls` file and invokes tree-sitter
2. UDL parser delegates tokenization to the external scanner for whitespace/comment/marker handling
3. Class structure is parsed top-down: class definition → members (methods, properties, etc.)
4. Method bodies are parsed using core grammar rules, which delegate to expr for expressions
5. XDATA blocks and embedded languages are identified
6. Injection queries detect language markers and apply sub-parsers (Python, SQL, XML, etc.)
7. Final AST with injection ranges is returned for syntax highlighting

---

## Assumptions

- Grammars are designed for tree-sitter with external scanner support
- Binding surface varies by language:
  - Node/Python/Rust expose `objectscript` and `objectscript_udl`
  - Go/Swift/C also expose `objectscript_core` and `objectscript_expr`
- Injection queries require corresponding tree-sitter grammars to be installed (SQL, HTML, Python, etc.)

## Open Questions

- Will CSP template support (HTML + ObjectScript injection) be added as a fifth grammar?
- Should the `expr` grammar be published as a standalone crate for SQL dialect extension?

## Evidence

- `tree-sitter.json:1-80` — Grammar configuration and binding definitions
- `expr/grammar.js:1-50` — Expression grammar structure
- `core/grammar.js:1-100` — Core grammar extending expr
- `udl/grammar.js:1-100` — objectscript_udl grammar extending core
- `objectscript/grammar.js:1-80` — objectscript playground grammar extending objectscript_udl
- `common/scanner.h` — External scanner implementation
- `udl/queries/injections.scm:1-150` — Language injection queries
- `README.md:1-111` — Project overview and editor integrations
