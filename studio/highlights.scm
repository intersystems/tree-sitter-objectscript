; NOTE that public local variables are highlighted
; differently than private

; variables in legacy procedures are highlighted differently than
; method procedures (unless ProcedureBlock=0)



;  "Object dot operator" and Dots are same color (black)
; but dots for a call to a method that is within the funciton (..) are different background

; &sql -> purple

